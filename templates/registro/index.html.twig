{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Registrar Usuario{% endblock %}

{% block body %}
	{{ parent() }}
	{% for message in app.flashes('exito') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
	{{ form(formulario) }}
{% endblock %}
